---
- name: Upgrade all Ubuntu VMs
  hosts: all
  user: linuxer
  sudo: yes

  tasks:
  - name: Update apt package cache and upgrade all packages
    ansible.builtin.apt:
      name: "*"
      update_cache: yes
      state:  latest
